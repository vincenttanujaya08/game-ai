import { NextRequest,NextResponse } from "next/server";import { append,getSession } from "@/server/session/repository";import { scoreSession } from "@/server/scoring/scorer";import { citationMini } from "@/server/scenario/citation-mini";
export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const{id}=await params, s=getSession(id), expected=Number(req.headers.get("if-match"));append(id,expected,[{clientEventId:crypto.randomUUID(),type:"session_submitted",occurredAt:new Date().toISOString(),payload:{responsibilityConfirmed:true}}]);return NextResponse.json({snapshot:s.snapshot,report:scoreSession(citationMini,s.events)})}catch{return NextResponse.json({error:{code:"SUBMISSION_FAILED",message:"Refresh and try again."}},{status:409})}}
